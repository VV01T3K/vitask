## Development

use vpr (pnpm under the hood)

- Check everything:

```bash
vpr check
```

- Try to fix everything:

```bash
vpr fix
```

- Build the monorepo:

```bash
vpr -r build
```

- Run the development servers:

```bash
vpr dev
```
